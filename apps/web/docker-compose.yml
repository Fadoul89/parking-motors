services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: parking
      POSTGRES_PASSWORD: parking
      POSTGRES_DB: parking_motors
    ports:
      - "5432:5432"
    volumes:
      - parking_pg_data:/var/lib/postgresql/data

volumes:
  parking_pg_data:
